# Superseded static site

These are the hand-written pages that `www.yukisrescue.org` served before
WordPress became the source of truth (2026-09-07).

They are kept for reference only. **Editing them has no effect** — production is
built from `site/`, which is generated by exporting WordPress at
`control.yukisrescue.org`.

`styles.css` was already dead before this move: no page referenced it.
